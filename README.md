# CSPM Tracker (Flask)

Backend e frontend estao em Flask (templates + static).

## Como rodar

1. `python -m venv .venv`
2. `.\.venv\Scripts\activate`
3. `pip install -r .\flask_app\requirements.txt`
4. `python .\flask_app\app.py`
